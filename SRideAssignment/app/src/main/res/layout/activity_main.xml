<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="app.srideassignment.model.WeatherInfo" />

        <import type="android.view.View" />

        <variable
            name="weatherInfo"
            type="WeatherInfo" />

        <variable
            name="loading"
            type="Boolean" />
    </data>


    <RelativeLayout

        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.cardview.widget.CardView
            android:id="@+id/card_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/card_margin"
            android:visibility="@{loading ? View.GONE : View.VISIBLE}"
            app:cardElevation="@dimen/card_elevation">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/location"
                    style="@style/WeatherInfo.Value"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@{@string/location(String.valueOf(weatherInfo.latitude), String.valueOf(weatherInfo.longitude)),default=`Location: 25.7572, 75.8796`}" />

                <TextView
                    style="@style/WeatherInfo.Value"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@{@string/temperature(String.valueOf(weatherInfo.currently.temperature)),default=`Temperature : 23`}" />

                <TextView
                    style="@style/WeatherInfo.Value"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@{@string/summary(String.valueOf(weatherInfo.currently.summary)),default=`Summary : 23`}" />

                <TextView
                    style="@style/WeatherInfo.Value"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@{@string/humidity(String.valueOf(weatherInfo.currently.humidity)),default=`Humidity : 23`}" />

                <TextView
                    style="@style/WeatherInfo.Value"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@{@string/wind(String.valueOf(weatherInfo.currently.windSpeed)),default=`WindSpeed : 23`}" />

            </LinearLayout>

        </androidx.cardview.widget.CardView>

        <Button
            android:id="@+id/btnCalender"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:background="@color/colorPrimary"
            android:text="@string/select_date"
            android:textColor="@color/textButton" />

        <ProgressBar
            android:id="@+id/progress_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:paddingBottom="@dimen/progress_bottom_margin"
            android:visibility="@{loading ? View.VISIBLE : View.GONE}" />

    </RelativeLayout>
</layout>